# Task: Fix Remaining Unreadable Visual in Homepage Features Section

## Metadata

- **ID:** TASK-0009
- **Created:** 2026-04-21
- **Updated:** 2026-04-25
- **Author:** Product owner (via requirement pack)
- **Status:** deployed
- **App(s):** app-3
- **Priority:** high
- **Type:** bug

## Deployment (2026-04-25)

- **Result:** live
- **Deployment basis:** user reported that all ANOTE-web tasks currently in `tested` are now deployed.
- **Sufficiency to move to `deployed`:** Yes. This task was already validated locally and is now treated as live in the intended environment per the deployment confirmation provided in this session.

## Validation (2026-04-25)

- **Result:** pass
- **Validation method:** local automated route/content verification against a running dev server at `http://localhost:3000`, plus `npx tsc --noEmit`, `npm run build`, and user confirmation that the change works correctly.
- **Routes verified:**
  - `/` returned `200`.
  - `/#features` rendered the updated first feature visual.
  - `/cenik` returned `200`.
  - `/faq` returned `200` and remained unchanged.
  - `/en` returned `200`.
  - `/en/pricing` returned `200`.
- **Implementation evidence recorded:**
  - `src/components/sections/Features.tsx` no longer renders the unreadable five-bar loading-style placeholder for the `transcript` visual.
  - The replacement visual now shows two readable mode cards aligned with the `Dva režimy přepisu` feature copy.
  - The rendered HTML contains the expected mode-card content tokens: `Whisper`, `gpt-4o`, `On-device`, and `0 KB out`.
- **Notes:**
  - The user explicitly confirmed the task is working correctly and asked to move it to `tested`.
  - No direct browser session was performed by this agent in the current turn, but the supplied validation evidence is sufficient for the project's `tested` state.
- **Sufficiency to move to `tested`:** Yes. The previously unreadable homepage feature visual was replaced, validated locally, and confirmed by the user.

## Problem

The homepage Features section still contains a visual that shows no readable content in the UI. The current `transcript` placeholder is five animated bars, which reads as a skeleton/loading state rather than meaningful product content.

## Desired Outcome

- The first feature visual on the homepage renders clear, meaningful content that users can actually read or interpret.
- The visual supports the surrounding feature copy instead of looking like an empty placeholder or loading state.
- The fix is validated visually in browser after implementation.

## Confirmed Facts

- The current raw HTML from the requirement pack matches the live `transcript` visual in `src/components/sections/Features.tsx`.
- `Features.tsx:13-18` renders five animated bars using `bg-primary/50`, `bg-primary/40`, and `bg-primary/30`.
- After TASK-0008, `featurePlaceholders` now contains only `"transcript"`, `"templates"`, and `"workflow"`, so the remaining problematic visual is the first item only.
- The later report captured as `TASK-0016` ("blank visual next to `Dva režimy přepisu`") maps to this same first feature slot: `features.items[0]` in the dictionary paired with `featurePlaceholders[0] = "transcript"` in `Features.tsx`.
- The current bars contain no actual text, labels, icons, or other semantically meaningful content. Even with stronger opacity, they still read as a decorative loading placeholder.
- The raw issue is therefore still reproducible despite TASK-0009 previously being moved to `tested`.

## Constraints

- Keep scope inside ANOTE-web only.
- Do not reintroduce removed feature items from TASK-0008.
- Prefer a small direct UI fix in `Features.tsx` over broader section redesign.
- Browser verification is required before returning this task to `tested`.

## Suspected Affected Areas

- `src/components/sections/Features.tsx` — replace or redesign the `transcript` visual
- `src/dictionaries/cs.json` and `src/dictionaries/en.json` — only if the replacement visual should render actual text from content files
- `src/app/globals.css` — only if a minimal supporting style is required

## Unknowns

- Whether the replacement should show actual transcript text, labeled mode chips, or another lightweight visual metaphor.
- Whether the current issue is considered solved by a clearer illustrative mock, or whether the product owner expects literal readable content inside the card.

## Notes

- **Reopened on 2026-04-21.** The previous validation result was incorrect or incomplete; the requirement pack confirms the visual is still not acceptable.
- Direct implementation candidate. No separate investigation task is needed.
- Confidence: **high** for the current location of the bug and the affected component.
- **Canonical task for this defect.** `TASK-0016` describes the same underlying homepage visual problem using different symptom wording, so implementation should be tracked under `TASK-0009`.

---

## Triage Assessment

| Field | Value |
|-------|-------|
| **Task ID** | TASK-0009 |
| **App(s) Impacted** | app-3 |
| **Task Type** | bug |
| **Priority** | high |
| **Urgency** | soon |
| **Impact** | medium — the homepage feature row still looks unfinished and weakens product communication |
| **Confidence** | high |
| **Dependencies** | None |
| **Recommended Status** | triaged |
| **Suggested Next Action** | Replace the `transcript` skeleton-bars placeholder in `Features.tsx` with a readable, intentional visual and re-test in browser. |

## Reasoning

This is a direct UI bug in a public-facing homepage section. The affected component and exact placeholder markup are already confirmed, so no additional investigation or spec step is required before implementation.

## Cross-App Considerations

None. Scope is limited to ANOTE-web.
