# Task: Remove Leftover '13 sekcí.' Text from Homepage Features Section

## Metadata

- **ID:** TASK-0008
- **Created:** 2026-04-21
- **Updated:** 2026-04-24
- **Author:** Product owner (via requirement pack)
- **Status:** deployed

## Deployment (2026-04-24)

- **Environment:** Azure Static Web Apps host `https://yellow-forest-086a45303.7.azurestaticapps.net`
- **Deployment status:** live
- **Lifecycle decision:** Move from `tested` to `deployed` because this ANOTE-web change is already live on the production host.
- **App(s):** app-3
- **Priority:** high
- **Type:** bug

## Problem

The text `13 sekcí.` (full title: `13 sekcí. Česká terminologie. Automaticky.`) remains visible as a feature item title on the homepage. This is leftover copy that should be removed.

## Desired Outcome

- The feature item with title `13 sekcí. Česká terminologie. Automaticky.` is no longer rendered on the homepage.
- The `features.items` array in the dictionary is updated accordingly.
- No broken layout or visual gaps remain.

## Confirmed Facts

- The text is a feature item title in `src/dictionaries/cs.json`, `features.items[1].title` (line 66 approx).
- The `Features` component maps over `dict.features.items` — removing or replacing the entry in the dictionary is the primary fix.
- The corresponding `featurePlaceholders` array in `Features.tsx` uses index-based mapping (`featurePlaceholders = ["transcript", "report", "templates", "workflow"]`). Removing index 1 from the items array will shift indices, which must be handled carefully to keep visuals aligned with the remaining items.

## Assumptions

- The entire feature item (title, body, badge) should be removed, not just the title text.
- After removal there will be 3 feature items remaining (was 4). The `featurePlaceholders` array in `Features.tsx` must be updated to match (remove index 1: `"report"`), or the mapping logic must be adjusted.

## Constraints

- Removing index 1 shifts all subsequent items down one index. Visual placeholder alignment must be verified after the change.
- Both `cs.json` and `en.json` must be updated if both contain the item.

## Suspected Affected Areas

- `src/dictionaries/cs.json` — remove `features.items[1]` entry
- `src/dictionaries/en.json` — remove equivalent entry
- `src/components/sections/Features.tsx` — update `featurePlaceholders` array to remove the `"report"` entry at index 1

## Unknowns

- Confirm whether `en.json` has an equivalent features items array and whether it contains the same item.
- Confirm final desired count of feature items (3 or different) with product owner if unclear.

## Notes

- Direct implementation candidate. No spec required.
- Closely related to TASK-0009 (Features section visual fix). Recommend implementing together in the same session to avoid double-editing the same files.
- Be careful with the `featurePlaceholders` index shift — test visually after the change.
- 2026-04-21: Verified locally in browser after implementation. Homepage visual checks passed; build and type checks had already passed.
