# Task: Pricing and FAQ Copy Corrections

## Metadata

- **ID:** TASK-0011
- **Created:** 2026-04-21
- **Updated:** 2026-04-24
- **Author:** Product owner (via requirement pack)
- **Status:** deployed

## Deployment (2026-04-24)

- **Environment:** Azure Static Web Apps host `https://yellow-forest-086a45303.7.azurestaticapps.net`
- **Deployment status:** live
- **Lifecycle decision:** Move from `tested` to `deployed` because this ANOTE-web change is already live on the production host.
- **App(s):** app-3
- **Priority:** high
- **Type:** bug

## Problem

The pricing and FAQ section contains incorrect or outdated copy that reflects a per-message pricing model. The product has moved to a monthly subscription model with a 7-day free trial. Several strings in the Czech dictionary are wrong and one FAQ item must be removed.

## Desired Outcome

All pricing-related copy updated to reflect: 7-day free trial → monthly subscription at a price to be announced. The hospital integration FAQ item removed.

## Confirmed Product Decisions

All of the following are confirmed by the product owner and ready for direct implementation.

### 1. FAQ — "Kolik to stojí?" answer

**Current (`cs.json` line 216):**
```
"Prvních 7 dní zdarma. Po zkušební době přibližně 0,03 Kč za zprávu. Žádné měsíční předplatné."
```

**Confirmed replacement wording:**
```
"7 dní zdarma. Poté měsíční předplatné. Cena bude oznámena."
```

### 2. FAQ item to remove

Remove the entire FAQ object (`cs.json` line 219):
```json
{ "q": "Mohu ANOTE napojit na svůj nemocniční systém?", "a": "V současné verzi můžete zprávu zkopírovat do schránky nebo odeslat emailem. API integrace s EHR/NIS systémy je v plánu." }
```

### 3. Pricing description copy

**Current (`cs.json` line 267):**
```
"7 dní zdarma. Potom přibližně 0,03 Kč za zprávu bez měsíčního předplatného."
```

**Required change:** Update to be consistent with the confirmed wording direction — retain the 7-day trial, replace per-message price with monthly subscription TBA messaging. Exact string should mirror the FAQ answer in spirit:
```
"7 dní zdarma. Poté měsíční předplatné. Cena bude oznámena."
```
(Exact wording for this field should be confirmed or matched to the FAQ answer above.)

### 4. Pricing section price field

**Current (`cs.json` line 161):**
```json
"price": "~0,03 Kč"
```

This field likely drives a visible price display in the `Pricing` component. Must be updated to reflect TBA / subscription model — e.g., `"Cena TBA"` or a similar placeholder. **Confirm with product owner** what should display in this UI field specifically (see Unknowns).

### 5. Pricing comparison table row

**Current (`cs.json` line 179):**
```json
{ "label": "Náklad na zprávu", "values": ["~0,03 Kč", "~150 Kč", "10–50 Kč"] }
```

Per-message pricing row is no longer valid. **Confirm with product owner** whether to remove this row, replace the label with a subscription-based comparison row, or update values only (see Unknowns).

### en.json

Both `cs.json` changes must be mirrored in `en.json` for any key that exists in both files. Confirm which fields exist in `en.json` before editing.

## Confirmed Affected Files

- `src/dictionaries/cs.json` — lines 161, 179, 216, 219, 267
- `src/dictionaries/en.json` — equivalent entries (verify line numbers before editing)
- `src/components/sections/FAQ.tsx` — verify no hardcoded strings; component should render from dict only
- `src/components/sections/Pricing.tsx` — verify no hardcoded strings; component should render from dict only

## Unknowns (still require product owner input before full implementation)

1. What should the `"price"` field (`cs.json` line 161) display in the UI? Options: `"Cena TBA"`, `"Předplatné"`, `"Brzy"`, or something else.
2. Should the `Náklad na zprávu` comparison table row be removed entirely, replaced with a new row (e.g., `"Měsíční předplatné"`), or the values updated?

The FAQ answer and the hospital integration FAQ removal are fully confirmed and can be implemented immediately without waiting for these answers.

## Notes

- **Partially implementation-ready.** Items 1 and 2 (FAQ answer update, FAQ item removal) can be implemented now.
- Items 3–5 (pricing description, price field, comparison table) should wait for the two open questions above, or use conservative TBA placeholders if the product owner confirms proceeding.
- No spec required.
- Confidence: **high** for confirmed items. The two unknowns are minor UI copy decisions.
- 2026-04-21: Verified locally in browser after implementation. Pricing and FAQ visual checks passed; build and type checks had already passed.
