# Task: Remove FAQ Section from `/cenik` Page

## Metadata

- **ID:** TASK-0017
- **Created:** 2026-04-24
- **Updated:** 2026-04-25
- **Author:** Product owner (via @task-triage request)
- **Status:** deployed
- **App(s):** app-3
- **Priority:** medium
- **Type:** bug

## Deployment (2026-04-25)

- **Result:** live
- **Deployment basis:** user reported that all ANOTE-web tasks currently in `tested` are now deployed.
- **Sufficiency to move to `deployed`:** Yes. This task was already validated locally and is now treated as live in the intended environment per the deployment confirmation provided in this session.

## Validation (2026-04-25)

- **Result:** pass
- **Validation method:** local automated route/content verification against a running dev server at `http://localhost:3000`, plus `npx tsc --noEmit`, `npm run build`, and user confirmation that the change works correctly.
- **Routes verified:**
  - `/cenik` returned `200` and no longer rendered a visible FAQ section between pricing content and the bottom CTA.
  - `/faq` returned `200` and still rendered `<h1>FAQ</h1>`.
  - `/en/pricing` returned `200`, confirming the shared route implementation still builds successfully for English.
- **Implementation evidence recorded:**
  - `src/app/[lang]/cenik/page.tsx` no longer imports the shared `FAQ` section component.
  - The route no longer constructs `pricingFaqIndices` or `pricingFaqDict`.
  - The wrapper section that previously rendered `<FAQ dict={pricingFaqDict} />` was removed.
  - Remaining `FAQ` matches on `/cenik` are limited to navigation links to `/faq` and serialized dictionary data in the RSC payload, which is expected and not a rendered FAQ section.
- **Notes:**
  - The user explicitly confirmed the task is working correctly and asked to move it to `tested`.
  - No direct browser session was performed by this agent in the current turn, but the supplied validation evidence is sufficient for the project's `tested` state.
- **Sufficiency to move to `tested`:** Yes. The pricing-page FAQ section is removed, the standalone FAQ page is unchanged, and local validation plus user confirmation cover the requested behavior.

## Problem

The `/cenik` page currently renders a `Často kladené otázky` section near the bottom of the page. This section should be removed from the pricing page.

The current implementation explicitly builds a pricing-page FAQ subset and renders it inside the pricing route component.

## Desired Outcome

- The `/cenik` page no longer renders the `Často kladené otázky` / FAQ section.
- The pricing page keeps its header, pricing content, and bottom CTA intact.
- The dedicated `/faq` page remains unchanged.
- Scope stays inside ANOTE-web only.

## Confirmed Facts

- `src/app/[lang]/cenik/page.tsx` imports the shared `FAQ` section component.
- `src/app/[lang]/cenik/page.tsx:60-68` constructs `pricingFaqDict` by selecting FAQ items from `dict.faq.items`.
- `src/app/[lang]/cenik/page.tsx:74-78` renders a dedicated section containing `<FAQ dict={pricingFaqDict} />`.
- The requested change targets the pricing page (`/cenik`), not the dedicated FAQ route.
- Scope is explicitly limited to ANOTE-web (`app-3`).

## Constraints

- Keep scope inside ANOTE-web only.
- Do not remove or alter the standalone `/faq` page.
- Prefer a minimal route-level removal over broader FAQ component or dictionary redesign.
- Ensure no empty spacing block remains where the FAQ section was previously rendered.

## Suspected Affected Files

- `src/app/[lang]/cenik/page.tsx` — remove the pricing-page FAQ subset construction and FAQ section render
- `src/dictionaries/cs.json` and `src/dictionaries/en.json` — likely no change needed unless unused pricing-only copy should be cleaned up later

## Unknowns

- Whether there is any pricing-page-specific analytics dependency tied to the FAQ block that should also be removed.
- Whether the English pricing route uses the same component path and therefore gets the removal automatically. Current code strongly suggests yes.

## Notes

- This is a direct implementation candidate. The pricing route already isolates the FAQ rendering in one file.
- The cleanest likely fix is to remove the `FAQ` import, `pricingFaqIndices`, `pricingFaqDict`, and the wrapper section that renders `<FAQ dict={pricingFaqDict} />`.
- Expected implementation repo: `/Users/ivananikin/Documents/ANOTE-web`.

---

## Triage Assessment

| Field | Value |
|-------|-------|
| **Task ID** | TASK-0017 |
| **App(s) Impacted** | app-3 |
| **Task Type** | bug |
| **Priority** | medium |
| **Urgency** | soon |
| **Impact** | low — this is a public-facing content/layout cleanup on the pricing page, but it does not affect core flows or correctness beyond page composition |
| **Confidence** | high |
| **Dependencies** | None |
| **Recommended Status** | triaged |
| **Suggested Next Action** | Remove the pricing-page FAQ block from `src/app/[lang]/cenik/page.tsx` and verify the pricing page layout still flows correctly without it. |

## Reasoning

The route component already contains the FAQ-specific logic and render block, so the change is well-scoped and ready for direct implementation without investigation or a spec. Priority is medium because this is a simple public-facing cleanup rather than a broken conversion path or demo failure.

## Cross-App Considerations

None. Scope is explicitly limited to ANOTE-web (`app-3`).
