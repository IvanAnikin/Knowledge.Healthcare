# Task: Health-Analysis repository foundation

## Metadata

- **ID:** TASK-0021
- **Created:** 2026-04-25
- **Updated:** 2026-04-26
- **Author:** Product owner (via @task-triage request)
- **Status:** tested
- **App(s):** app-4
- **Priority:** high
- **Type:** infra

## Validation (2026-04-26)

- **Result:** pass
- **Validation method:** user-supplied full-stack validation summary after completion of TASK-0021 through TASK-0026.
- **Validation evidence reported:**
  - `cd api && source .venv/bin/activate && pytest` -> `49 passed`
  - `cd api && source .venv/bin/activate && ruff check .` -> clean
  - `cd web && npm test` -> `1 passed`
  - `cd web && npx tsc --noEmit` -> clean
  - `cd web && npm run test:e2e` -> `4 passed`
- **What this confirms for TASK-0021 specifically:**
  - The repository foundation and monorepo structure remain sound under the now-complete app-4 MVP workflow.
  - The repo-local `.venv` and documented command flow work for the backend test suite.
  - Frontend tooling and test commands introduced from the repo foundation continue to support the implemented app surface.
- **Sufficiency to move to `tested`:** Yes. Later end-to-end verification across the completed app validates that the foundation introduced by TASK-0021 is working in practice.

## Completion Status

- **Result:** done
- **Commit:** `8638b77`
- **What was completed:**
  - Root repo foundation created: `.gitignore`, `.env.example`, `.editorconfig`, `.pre-commit-config.yaml`, `README.md`, `ci.yml`
  - Backend foundation created under `api/`: `pyproject.toml`, `README.md`, `app/__init__.py`, `app/config.py`, `app/main.py`, `tests/__init__.py`, `tests/test_health.py`
  - Frontend foundation created under `web/`: `package.json`, `tsconfig.json`, `next.config.js`, `.eslintrc.json`, `next-env.d.ts`, `vitest.config.ts`, `vitest.setup.ts`, `app/layout.tsx`, `app/page.tsx`, `app/page.test.tsx`, `README.md`
  - Synthetic sample data created under `samples/cgm/`: `libreview-sample.csv`, `README.md`
- **Validation summary:** The implementation agent reported that the baseline validation commands completed successfully and that the repo can boot at the M0 level for both API and web.
- **Documented deviations:**
  - Only the directories needed to boot at `M0` were created; deeper subtrees from `docs/ARCHITECTURE.md` were intentionally deferred to later milestones to avoid unnecessary abstractions.
  - Frontend baseline testing uses Vitest + React Testing Library instead of Playwright at this stage, which is appropriate for placeholder-level M0 validation.
  - Local development used Python `3.14`, while repo and CI targets remain Python `3.11`, matching the documentation.
- **Useful follow-ups for TASK-0022:**
  - Replace the placeholder homepage with the upload flow when `M1` begins.
  - Add `app/api/v1/uploads.py` and wire it into `app/main.py` via `include_router`.
  - Add `python-multipart`, `pandas`, and `openpyxl` to `api/pyproject.toml` when upload/preview work starts.
  - `.env.example` already contains future variables as comments; uncomment only when those milestones land.
- **Remaining validation boundary:** No blocker reported for proceeding to `TASK-0022`.

## Problem

`Health-Analysis` has a tested documentation foundation but no actual repository scaffolding yet. The monorepo structure, baseline tooling, synthetic sample data, and CI foundations need to exist before product work can proceed cleanly.

## Desired Outcome

- The repository is initialized as a monorepo rooted at `/Users/ivananikin/Documents/Health-Analysis`.
- `api/` contains a minimal FastAPI skeleton.
- `web/` contains a minimal Next.js skeleton.
- Root repo setup includes `.env.example`, `README.md`, lint/format configuration, `pre-commit`, and CI.
- A synthetic CGM sample file exists for development and tests.

## Scope

- Initialize git repository.
- Create `api/` FastAPI skeleton.
- Create `web/` Next.js skeleton.
- Add `.env.example`, `README.md`, lint/format configs, `pre-commit`, and CI.
- Add synthetic CGM sample file under `samples/cgm/`.

## Constraints

- Keep the repo as a monorepo (`api/` + `web/`).
- Match the architecture and folder-shape decisions already documented in `docs/ARCHITECTURE.md` and `docs/IMPLEMENTATION_PLAN.md`.
- No product logic yet beyond skeletons and sample data.
- Keep secrets out of the repo.

## Validation

- `pytest` runs cleanly with placeholder tests.
- Frontend test/lint baseline runs cleanly with placeholder coverage.
- `uvicorn` starts the API skeleton.
- `next dev` starts the web skeleton.
- CI config is present and wired to run the baseline checks.

## Notes

- This is the implementation equivalent of Milestone `M0` in `Health-Analysis/docs/IMPLEMENTATION_PLAN.md`.
- Expected repo: `/Users/ivananikin/Documents/Health-Analysis`.

---

## Triage Assessment

| Field | Value |
|-------|-------|
| **Task ID** | TASK-0021 |
| **App(s) Impacted** | app-4 |
| **Task Type** | infra |
| **Priority** | high |
| **Urgency** | immediate |
| **Impact** | high — this unlocks all subsequent app-4 implementation work |
| **Confidence** | high |
| **Dependencies** | TASK-0020 tested docs |
| **Recommended Status** | triaged |
| **Suggested Next Action** | Execute TASK-0021 first to establish the monorepo foundation before building feature slices. |

## Reasoning

This is the correct first execution task because every later slice assumes a working monorepo, baseline tooling, and a synthetic CGM sample for development and tests. The scope is straightforward and directly mapped from the implementation plan, so it is ready for execution without another spec step.

## Cross-App Considerations

None. Scope is limited to `Health-Analysis` (`app-4`).
