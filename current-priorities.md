# Current Priorities

> Last updated: 2026-05-01

## Active Goals

- **Resolve production domain** — Replace all `// TODO: replace with production domain` placeholders (Azure hostname) across ANOTE-web once `anote.cz` goes live.
- **Advance App 4 beyond MVP completion** — `Health-Analysis` MVP path is fully tested; the four post-MVP slices (`TASK-0030` Broz CSV + monthly/post-meal views, `TASK-0031` progressive analysis page + foldable Episodes table, `TASK-0032` Czech i18n + language switch, `TASK-0033` Broz/LibreView Czech CSV auto-mapping) are all `tested` as of 2026-04-29. **TASK-0034** (mandatory Czech safety disclaimer beneath dose-modification recommendations in Diabetický poradce 2 / app-1) is **deployed** to `medical-advisor-cz` as of 2026-05-01 — verified live in production. Remaining app-4 follow-ups: clinician review of the 8 `// TODO(i18n-review)` Czech strings; full Playwright e2e on the Broz upload path (analysis-page snapshot doesn't intersect it).
- **Validate TASK-0019 logo rollout on real devices** — confirm new favicon and mobile app icons render correctly on a deployed ANOTE-web build, an Android device/emulator (now using a proper adaptive icon, so verify launcher mask shape behavior across at least one stock-Android and one OEM device), and the iOS Simulator after a clean build.

## In Progress

- Nothing currently in progress.

## Blockers

- **No verified automated test results for any application.** Test counts conflict across documentation (App 1: README says 57, IMPLEMENTATION_STATUS says 32, 8 test classes found). Need `dotnet test`, `pytest`, and `flutter test` runs.
- **No monitoring or observability** across any application — no Application Insights, no centralized logging, no alerting. Blocks operational confidence.
- **Security follow-up pending:** rotate the leaked GitHub PAT `ghp_kPAeVa…Y0t85zz` previously exposed in terminal output.

## Next Recommended Actions

1. **Commit the `medical_advisor` working tree** — TASK-0034 was deployed from an uncommitted working tree. Commit the current state with a message capturing TASK-0034 and any other local changes so the deployed production state is traceable in version control.
2. **Validate TASK-0019** on real surfaces — browser favicon on deployed ANOTE-web, Android launcher icon on a device/emulator after `flutter clean && flutter run`, iOS app icon on the iOS Simulator.
3. **Provide a monochrome Android notification small icon master** for the new ANOTE logo and wire it into the `flutter_foreground_task` ForegroundService — deferred from `TASK-0019`.
4. **Clinician review of 8 `// TODO(i18n-review)` Czech strings** in `web/lib/i18n/dictionaries/cs.ts` (TASK-0032 follow-up) — uncertain clinical wording flagged but not gating.
5. **Full Playwright e2e on the Broz upload path** (TASK-0033 follow-up) — analysis-page snapshot doesn't intersect the upload flow; backend pytest 69/69 covers the contract but a visual upload-flow pass is still owed.
6. **Run/record broader application test suites** — capture verified pass/fail counts for the other apps as well, not just app-4.
7. **Rotate the leaked GitHub PAT** `ghp_kPAeVa…Y0t85zz` immediately at `https://github.com/settings/tokens` and update any dependent automation if needed.
8. **Replace production domain placeholders** — when `anote.cz` is confirmed live, sweep all `// TODO: replace with production domain` comments across ANOTE-web (9 locations).
9. **Verify App 2 backend region** — confirm whether it is still West US 2 or has been migrated to West Europe.
10. **Document the shared `anote-openai` resource** as a standalone infrastructure page.
11. **Address the hardcoded bearer token in App 2 mobile source** before any broader distribution.

### App-3 Follow-Ups

- **TASK-0019 done (cross-app)** — ANOTE-web favicon and ANOTE_mobile Android/iOS launcher icons updated to the new ANOTE logo. **Addendum 2026-04-27:** rounded corners baked into web icons; Android upgraded to a proper adaptive icon (foreground PNGs at 5 densities + `ic_launcher_background` color resource + `mipmap-anydpi-v26/ic_launcher.xml`); iOS deliberately left square per Apple HIG. Real-device/browser validation and a brand-supplied monochrome notification icon for Android remain as follow-ups.
- **TASK-0027 deployed** — the homepage `Features.tsx` anamneza animation expansion is now reported live on Azure, showing eight chips (`NO`, `RA`, `OA`, `FA`, `AA`, `GA`, `SA`, `EA`) with no `BouncingChips.tsx` changes required.
- **TASK-0028 deployed** — the dedicated `/bezpecnost` page fix is now reported live on Azure, keeping only the route-level `PageHeader`, removing the duplicate `Privacy.tsx` heading/subheading, and tightening the top spacing above the shield.
- **TASK-0018 deployed** — the duplicate heading fix is now reported live. Root cause was the route-level `PageHeader` plus a second internal heading inside `Pricing.tsx`; removing the inner heading block resolved both `/cenik` and `/en/pricing` while preserving spacing.
- **TASK-0014 deployed** — all five child phases (`A` through `E`) are reported shipped to production on 2026-04-25. App 3 now runs on the ANOTE-web-owned `anote-web-api` backend image `0.2.0`, with warm scale pinned, pre-warm/retry, VAD silence filtering, SSE report streaming, stale-cancel/debounce, and the Sweden Central `gpt-4o-mini-transcribe` endpoint live.
- **TASK-0009 deployed** — the homepage `Features.tsx` `transcript` slot fix is now reported live after prior local validation and user confirmation.
- **TASK-0012 deployed** — the dedicated `/bezpecnost` page and related routing/IA fix are now reported live after prior automated local validation.
- **TASK-0017 deployed** — the `/cenik` FAQ removal is now reported live after prior local validation and user confirmation.

### App-4 Follow-Ups

- **TASK-0020 tested** — the initial `Health-Analysis` repository documentation package is now reported complete, including architecture, roadmap, safety/governance, technical specification, and milestone-based implementation planning.
- **TASK-0021 tested** — repository foundation is now covered by later full-stack verification across the completed app flow.
- **TASK-0022 tested** — ingest/preview/mapping is now exercised successfully in the validated end-to-end user flow.
- **TASK-0023 tested** — deterministic CGM validation and normalization are now exercised successfully in the validated end-to-end user flow.
- **TASK-0029 tested** — the app-4 frontend redesign is now reported implemented and validated. `Health-Analysis` uses a shared dark-mode-first visual system with glass panels, rounded surfaces, ambient background lights, a persisted theme toggle, clearer step-based upload flow, summary-first analysis layout, theme-aware charts, and reduced-motion support.
- **TASK-0030 tested** — Broz LibreView CSV variant ingest, `PostMealEvent`/`RecentMealEvents` outputs (14-day window, ±12-min tolerance, `MODULE_VERSION` 1.0.0→1.1.0), monthly-comparison + recent-meals tables. Promoted from `done` after the 2026-04-29 validation pass refreshed the Playwright snapshot to bake in the new tables and re-confirmed the Broz pipeline smoke (`module_version=1.1.0`, 1955 readings, 57 meals, 57 recent events).
- **Recommended execution order now:** TASK-0034 is fully deployed. Next app-1 follow-up: commit the `medical_advisor` working tree so the deployed state is in version control. App-4 post-MVP slice (TASK-0030/0031/0032/0033) is fully `tested`.
- **TASK-0024 tested** — the deterministic CGM analytics core is complete and backend-validated.
- **TASK-0025 tested** — the analysis page and 7-chart presentation layer are complete and manually usable end to end locally.
- **TASK-0026 tested** — the LLM summary and audit layer is now implemented end to end, including Azure OpenAI wiring, prompt contract enforcement, file-backed audit persistence, and the analysis-page summary panel.
- **TASK-0031 tested (2026-04-29)** — progressive top-down rendering with dynamic `ChartPanel` + `ChartSkeleton`; foldable `EpisodesTable` disclosure with windowing at 200 / slow-warn at 1000. Validation gate: `pytest` 69/69, vitest 18/18 (incl. 5 new `EpisodesTable.test.tsx` cases), Playwright 4/4, snapshot regenerated (5647→7857 px). Two stale `analysis.spec.ts` assertions repaired alongside the refresh (`cgm v1.0.0`→`v1.1.0` and heuristic-badge locator).
- **TASK-0032 tested (2026-04-29)** — in-house ~30-LOC i18n at `web/lib/i18n/`, segmented EN\|CS `LanguageSwitch`, locale-aware formatters (en byte-identical, cs `Intl` with comma decimals). 8 `// TODO(i18n-review)` Czech-clinical-wording markers + LLM summary localization remain as deliberately-deferred follow-ups.
- **TASK-0033 tested (2026-04-29)** — exact-match + diacritic-sensitive Czech-header detection, `UploadResponse.suggested_mapping`, frontend `buildSeededMapping` + auto-detected/auto-mapped badges. Caveat: full Playwright e2e on the Broz upload path is still a separate follow-up — analysis-page snapshot doesn't intersect the upload flow; backend pytest 69/69 covers the contract.
- **TASK-0034 triaged (2026-04-29)** — mandatory Czech safety disclaimer beneath dose modification recommendations in Diabetický poradce 2 (app-1). Recommended status `needs-spec`.
- **TASK-0034 implemented & tested locally (2026-04-30)** — server-side enforcement (not prompt-based) in `ChatMessage.cs`, `MedicalAdvisorService.cs`, new `DoseWarningHelpers.cs`, and `ChatMessageBubble.razor`. Build: 0 errors, 0 warnings (6.57 s). Tests: 104 passed (96 existing + 10 new).
- **TASK-0034 deployed (2026-05-01)** — live on Azure App Service `medical-advisor-cz` (https://medical-advisor-cz.azurewebsites.net). Deployment ID `e7d0e90f-d415-4529-be7f-08c6d6ebf3ae`. Production check confirmed full Czech disclaimer including "na rozdíl o vašeho lékaře" ✅. Caveat: deployed from an uncommitted working tree; follow-up git commit recommended.

## Recently Completed

- 2026-05-01: TASK-0034 deployed to production. `az webapp deploy` (ZIP) to Azure App Service `medical-advisor-cz`. Deployment ID `e7d0e90f-d415-4529-be7f-08c6d6ebf3ae`. Azure state: Running. `dotnet test`: 104 passed, 0 failed. Live production check confirmed the full verbatim Czech safety disclaimer ("na rozdíl o vašeho lékaře") renders beneath dose-modification recommendations in Diabetický poradce 2. Caveat: deployed from a working tree with uncommitted local changes beyond TASK-0034; TASK-0034 itself is verified working; a git commit to capture the deployed state is a recommended follow-up.

- 2026-04-29: TASK-0030, TASK-0032, TASK-0033 promoted from `done` to `tested` after the 2026-04-29 validation gate. Closes the post-MVP app-4 slice (Broz CSV ingest, monthly/post-meal views, Czech i18n, automatic field mapping). Two stale assertions in `web/e2e/analysis.spec.ts` were repaired during the gate; an explicit `npm run test:e2e` requirement was added to `workflows/session-end-checklist.md` for app-4 frontend work to prevent the same drift.
- 2026-04-29: TASK-0031 moved to `tested`. Validation: `pytest` 69/69, vitest 18/18 (3 files), Playwright 4/4 post snapshot refresh, `analysis-page-chromium-darwin.png` regenerated (5647→7857 px reflecting collapsed Episodes disclosure, Monthly comparison + Recent meals tables, EN\|CS header, ChartSkeleton placeholder).
- 2026-04-29: Triaged TASK-0034 (mandatory Czech safety disclaimer beneath dose-modification recommendations in Diabetický poradce 2). Single-app (app-1, `medical_advisor`, `diabetes-2` advisor). Safety-relevant. Recommended status `needs-spec`.

- 2026-04-27: TASK-0019 addendum shipped. Web icons (`favicon.ico`, `icon.png`, `apple-icon.png`, `icon.svg`) regenerated with 22.5% rounded corners and transparent corners. ANOTE_mobile Android upgraded from a flat launcher icon to a full adaptive icon: added `mipmap-anydpi-v26/ic_launcher.xml` and `ic_launcher_round.xml`, `values/ic_launcher_background.xml` with color `#FFFFFF`, and `ic_launcher_foreground.png` at 5 mipmap densities (108/162/216/324/432 px, wordmark inside the 66dp safe zone). Legacy `ic_launcher.png` re-baked with rounded corners as Android <8 fallback. iOS deliberately left square (Apple HIG: opaque + system superellipse mask). Re-validation: `npm run build` clean for ANOTE-web with all four icon routes registered; `flutter analyze` baseline unchanged at 14 pre-existing issues (1 in `lib/services/whisper_service.dart`, 13 in `test/**`).

- 2026-04-27: `TASK-0030` moved to `done` after the user supplied a full implementation report. Backend changes: content-detected double-quoted-line preprocessor in `loaders.py` for the Czech LibreView variant; new `PostMealEvent`/`RecentMealEvents` models in `analytics/cgm/results.py`; `compute_recent_meal_events` plus helpers and constants `RECENT_MEALS_WINDOW_DAYS=14` / `RECENT_MEALS_OFFSET_TOLERANCE_MINUTES=12` in `analytics/cgm/post_meal.py`; wiring + assumption line + `MODULE_VERSION` bump `1.0.0` → `1.1.0` in `analytics/cgm/module.py`; new `tests/test_broz_pipeline.py`. Frontend changes: `PostMealEvent`/`RecentMealEvents` types and `recent_meal_events` field in `lib/api.ts`; new transposed `MonthlyComparisonTable` and `RecentMealsTable` in `components/StatsTables.tsx`; analysis page rendering both. Reported validation: `pytest -q` `58 passed` (7 new), `npm run lint`/`typecheck`/`test --run` (`1/1`)/`build` all clean, REPL smoke on `Test1.csv` produced `module_version=1.1.0`, 1955 readings, 57 meals, 57 recent events. Playwright snapshot refresh under `web/e2e/analysis.spec.ts-snapshots/` is the remaining gate to `tested`.

- 2026-04-26: `TASK-0019` moved to `done`. The new ANOTE logo (sourced from `ANOTE_mobile/logo.PNG`, 424x424 RGBA, fully opaque) was propagated to ANOTE-web (`favicon.ico` regenerated multi-size, plus new `icon.png` 512, `apple-icon.png` 180 RGB, and `icon.svg` mirroring `logo.svg`) and to ANOTE_mobile Android (all 5 mipmap densities of `ic_launcher.png`) and iOS (all 15 `AppIcon.appiconset` entries flattened to RGB, including the 1024x1024 marketing icon). Validation: `npx tsc --noEmit` and `npm run build` clean on ANOTE-web with the new icon routes registered; `flutter analyze` produced only pre-existing test-only `info` lints. The Android status-bar/notification small icon was intentionally not generated because it requires a brand-approved monochrome silhouette master and is now tracked as a follow-up.
- 2026-04-26: `TASK-0024` and `TASK-0025` moved to `tested` after the user reported implementation plus validation. `TASK-0024` now has a versioned deterministic CGM analytics core with reproducibility-focused backend tests, and `TASK-0025` now provides the end-to-end local analysis surface with `/api/v1/analyses`, statistics tables, and 7 Plotly charts rendering from deterministic results.
- 2026-04-26: `TASK-0027` and `TASK-0028` moved to `deployed` after the user confirmed both ANOTE-web changes are already live on Azure. `TASK-0027` adds the expanded 8-chip anamneza animation on the homepage features section, and `TASK-0028` removes the duplicate security-page heading while tightening the shield top spacing.
- 2026-04-26: `TASK-0029` moved to `tested` after the user reported implementation and validation of the app-4 redesign. Reported outcomes include a new shared dark-mode-first visual system in `web/app/globals.css`, sticky glass app shell and persisted theme behavior in `layout.tsx`, a new `ThemeToggle.tsx`, step-based upload flow on `/`, summary-first analysis hierarchy on `/analysis/[id]`, theme-aware chart presentation, and reduced-motion support. Reported validation: `npm run lint`, `npm run typecheck`, `npm test` (`1/1 passed`), and `npm run build` (`4/4 pages generated`).
- 2026-04-26: `TASK-0021`, `TASK-0022`, `TASK-0023`, and `TASK-0026` moved to `tested` after the user supplied final validation evidence for the completed app-4 MVP. Reported evidence includes backend `pytest` (`49 passed` before the final LLM slice and `51 passed` after it), backend `ruff check .` clean, frontend unit/type checks clean, `npm run test:e2e` with `4 passed`, and manual confirmation that the implementation works well end to end. `TASK-0026` now adds the Azure OpenAI-backed summary layer, citation-checked prompt contract, file-backed audit log, and analysis-page LLM summary panel.
- 2026-04-26: Triaged `TASK-0030` for `Health-Analysis` based on the new `samples/broz/` LibreView CSV set and customer feedback requesting richer month-by-month comparison tables plus inferred post-meal analysis over recent meals. The task is based on the sample CSVs and pasted notes only; the referenced PDF/image attachment could not be read directly in this session.
- 2026-04-26: `TASK-0028` moved to `tested` after the user reported the `/bezpecnost` duplicate heading fix works well. The implementation reportedly removed the inner `Privacy.tsx` heading/subheading block, reduced top padding from `py-24 sm:py-32` to `pt-12 pb-24 sm:pt-16 sm:pb-32`, increased shield spacing from `mb-4` to `mb-16`, and passed both `npx tsc --noEmit` and `npm run build`.
- 2026-04-26: Triaged `TASK-0028` for ANOTE-web to remove the duplicate heading/subheading on the dedicated security page and tighten the top spacing above the shield icon. Direct repo inspection confirmed `/bezpecnost` renders a route-level `PageHeader` plus a second heading block inside `src/components/sections/Privacy.tsx`, and that `Privacy.tsx` is only used by the shared `[lang]/bezpecnost` route.
- 2026-04-26: `TASK-0027` moved to `tested` after the user reported implementation and validation of the expanded homepage anamneza chip set in `Features.tsx`. The chip list now reportedly includes `NO`, `RA`, `OA`, `FA`, `AA`, `GA`, `SA`, and `EA`; `npx tsc --noEmit` and `npm run build` both passed, while only a non-blocking browser comfort check remains.
- 2026-04-25: TASK-0023 moved to `done` after the user reported completion of deterministic CGM validation and normalization in `Health-Analysis`, including structured validation issues, LibreView record-type handling, mmol/L canonicalization, the validation endpoint, frontend validation UI, and a normalized DataFrame stored on the upload record for analytics. No blockers reported for `TASK-0024`.
- 2026-04-26: Triaged `TASK-0027` for ANOTE-web to expand the homepage anamneza `BouncingChips` visual with a larger shortcut set. Direct repo inspection confirmed the current implementation lives in `src/components/sections/Features.tsx` and `src/components/animations/BouncingChips.tsx`, with the current five-chip list passed as data.
- 2026-04-25: TASK-0022 moved to `done` after the user reported completion of the ingest/preview/mapping slice in `Health-Analysis`, including CSV/XLSX loaders, upload/preview/mapping endpoints, an in-memory no-source-byte-retention upload store, typed frontend API client, and a single-page upload -> preview -> mapping flow. No blockers reported for `TASK-0023`.
- 2026-04-25: Moved all current app-3 `tested` tasks (`TASK-0009`, `TASK-0012`, `TASK-0017`, `TASK-0018`) to `deployed` after the user confirmed they are now live.
- 2026-04-25: `TASK-0018` moved to `tested` after the user-supplied validation summary confirmed the duplicate pricing-page heading was fixed in both locales by removing the inner `Pricing.tsx` heading block. Root cause was stacked route-level and section-level headings; `npx tsc --noEmit` and `npm run build` both passed and spacing remained correct.
- 2026-04-25: TASK-0014 moved to `deployed` after the user reported all five child phases shipped and live on App 3 production. Confirmed outcomes include `anote-web-api` split to its own backend image/repo, `minReplicas=1`, pre-warm plus single 5xx retry, VAD silence filtering, SSE `/report?stream=1` contract, stale-cancel/debounce, and `AZURE_WHISPER_ENDPOINT` migration to `gpt-4o-mini-transcribe` on `anote-openai-swe`. Production validation reported `/health` 200 in ~0.35 s, `/report?stream=1` TTFT 0.38 s / total 3.13 s, and legacy JSON `/report` still returning 200 in 2.62 s.
- 2026-04-25: TASK-0021 moved to `done` after the user reported repo foundation completion in `Health-Analysis`, committed at `8638b77` on `main`, with monorepo root files, minimal FastAPI and Next.js skeletons, baseline tooling/CI, and a synthetic CGM sample. No blockers reported for `TASK-0022`.
- 2026-04-25: Moved `TASK-0021` to `active` as the first app-4 execution task, making `Health-Analysis` monorepo foundation setup the current implementation focus.
- 2026-04-25: Triaged six sequenced app-4 implementation tasks (`TASK-0021` through `TASK-0026`) from the `Health-Analysis` implementation plan, covering repo foundation, ingest/mapping, validation/normalization, CGM analytics core, charts/UI, and LLM summary/audit.
- 2026-04-25: TASK-0020 moved to `tested` after the user-supplied implementation summary confirmed all six `Health-Analysis` foundation docs were created with Mermaid architecture, milestone-based implementation planning, phased roadmap, coherent stack recommendation, CGM-first MVP scope, safety framing, and documented risks/open questions.
- 2026-04-25: Registered new application `app-4` (`Health-Analysis`) in the knowledge layer, created `/Users/ivananikin/Documents/Health-Analysis`, and triaged TASK-0020 for its initial project documentation package.
- 2026-04-25: Added minimal repo-local `AGENTS.md` files to `ANOTE_mobile` and `medical_advisor` that point contributors back to the shared knowledge-base instructions and Azure CLI access runbook.
- 2026-04-25: Added `workflows/azure-cli-access.md` and updated agent instructions to record that Azure CLI may not be globally available; the current known note is that `az` is user-reported to be available from the `ANOTE_mobile` pip virtual environment.
- 2026-04-25: Triaged new cross-app branding task `TASK-0019` to replace the ANOTE logo across ANOTE-web favicon assets and ANOTE_mobile Android/iOS app icons using the supplied logo artwork as the candidate source asset.
- 2026-04-25: TASK-0009 moved to `tested` after local validation on the running dev server confirmed the homepage transcript visual now renders readable mode cards, `npx tsc --noEmit` and `npm run build` passed, and the user confirmed the fix works correctly.
- 2026-04-25: TASK-0017 moved to `tested` after local validation on the running dev server confirmed `/cenik` no longer renders the FAQ section, `/faq` remained unchanged, `npx tsc --noEmit` and `npm run build` passed, and the user confirmed the fix works correctly.
- 2026-04-24: Removed duplicate task `TASK-0016` after folding its relevant symptom detail into `TASK-0009`. The canonical homepage visual task remains `TASK-0009`.
- 2026-04-24: TASK-0012 moved to `tested` after automated local validation confirmed the dedicated `Bezpečnost` page, corrected CTA routing, English `/en/security` rewrite, removal from `typy-zprav`, and sitemap coverage.
- 2026-04-24: TASK-0015 completed. Marketing brief artifacts are now stored under `applications/` as `anote-marketing-brief.md`, `anote-marketing-brief.txt`, and `anote-marketing-brief.pdf`. The markdown file remains the canonical editable source; the PDF could not be read directly in this session because the current model does not support PDF input.
- 2026-04-24: Moved all current app-3 `tested` tasks (`TASK-0001`, `TASK-0002`, `TASK-0003`, `TASK-0004`, `TASK-0005`, `TASK-0007`, `TASK-0008`, `TASK-0010`, `TASK-0011`, `TASK-0013`) to `deployed` because they are already live on `https://yellow-forest-086a45303.7.azurestaticapps.net`.
- 2026-04-24: Production validation on Azure SWA (`yellow-forest-086a45303.7.azurestaticapps.net`, commit `c494a02`, workflow run `24884960261`) confirmed TASK-0001, TASK-0003, TASK-0004, TASK-0010, and TASK-0013. All five tasks are now in `tested`.
- 2026-04-19: Initial inspection of all 3 application repositories; `app-1-overview.md`, `app-2-overview.md`, `app-3-overview.md` written.
- 2026-04-19: `system-landscape.md` and `glossary.md` populated from cross-repo synthesis.
- 2026-04-20: `project-overview.md` populated with confirmed mission, users, scope, risks, and compliance concerns.
- 2026-04-20: `current-priorities.md` populated.
- 2026-04-20: Synthesis handoff note written summarizing full project understanding.
- 2026-04-20: TASK-0001, TASK-0002, TASK-0004, TASK-0005 — complete content/copy/structural batch implemented in ANOTE-web. Build and type-check pass cleanly (28 static pages).
- 2026-04-20: TASK-0003 — Anamnéza floating animation (BouncingChips) and workflow fork diagram implemented in ANOTE-web. Build and type-check pass.
- 2026-04-21: Local browser verification completed for TASK-0007, TASK-0008, TASK-0009, and TASK-0011 in ANOTE-web. Visual checks passed; task statuses advanced to `tested`.
- 2026-04-21: TASK-0010 remains in `done` pending production deployment verification of CSP behavior.
- 2026-04-21: TASK-0009 reopened after a new requirement pack confirmed the homepage `transcript` visual still shows unreadable placeholder bars.
- 2026-04-21: TASK-0012 reframed from a new feature to a direct bugfix/content-architecture correction because `Bezpečnost` currently lives inside `typy-zprav`.
- 2026-04-23: Local production build + browser validation completed for TASK-0001, TASK-0002, TASK-0003, TASK-0004, TASK-0005, and TASK-0010 in ANOTE-web.
- 2026-04-23: TASK-0002 and TASK-0005 advanced to `tested` after local validation passed.
- 2026-04-23: TASK-0001, TASK-0003, and TASK-0004 were reopened to `active` after local validation found mismatches against their task definitions.
- 2026-04-23: TASK-0010 stayed in `done`; local checks were useful but could not confirm Azure SWA production CSP behavior.
- 2026-04-23: TASK-0013 moved to `done` after investigation confirmed the primary cause was `anote-web-api` model deployment configuration, not ANOTE-web code. The Azure Container App env was aligned to `gpt-4-1-mini` with `gpt-5-nano` fallback, reducing direct `/report` latency from 19.2 s to 4.7-6.3 s.
- 2026-04-23: TASK-0001 rework completed and moved back to `done`; dictionary fixes restored the required FAQ item, removed the certification-disclaimer sentence from demo/terms copy, and passed `npx tsc --noEmit`, `npm run build`, and prerendered HTML verification.
- 2026-04-23: TASK-0003 rework completed and moved back to `done`; `BouncingChips.tsx` now provides the required floating/bouncing anamnéza-chip visual in `Features.tsx`, and `npx tsc --noEmit` plus `npm run build` pass cleanly. Browser validation is still needed before moving to `tested`.
- 2026-04-23: TASK-0004 rework completed and moved back to `done`; `src/app/[lang]/impressum/` was deleted, dead `impressum` dictionary keys were removed, README references were scrubbed, and `rm -rf .next && npm run build` confirmed no `/[lang]/impressum` route is emitted.
- 2026-04-23: Local validation pass promoted TASK-0001 and TASK-0004 to `tested`; TASK-0003 stayed in `done` because animation behavior still needs interactive browser checks; TASK-0010 and TASK-0013 stayed in `done` because their remaining gates are deployed-production verification.
